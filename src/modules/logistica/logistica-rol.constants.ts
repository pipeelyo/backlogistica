/** IDs tras `database/11-seed-rol.sql` (tabla vacía). */
export const ROL_ID_CLIENTE = 1;
export const ROL_ID_REPARTIDOR = 2;
export const ROL_ID_ADMINISTRADOR = 3;
export const ROL_ID_SUPERVISOR = 4;

/** Códigos lógicos (no hay columna `codigo` en `rol`; se comparan por `nombre` o por id). */
export const ROL_CODIGO_CLIENTE = 'CLIENTE';
export const ROL_CODIGO_REPARTIDOR = 'REPARTIDOR';
export const ROL_CODIGO_ADMINISTRADOR = 'ADMINISTRADOR';
export const ROL_CODIGO_SUPERVISOR = 'SUPERVISOR';
